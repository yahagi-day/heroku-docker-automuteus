setup:
  addons:
  - plan: heroku-postgresql:hobby-dev
  - plan: heroku-redis:hobby-dev
  config:
    DISCORD_BOT_TOKEN:
    HEROKU_APP_NAME:
build:
  docker:
    web: web/Dockerfile
  config:
    AUTOMUTEUS_TAG: 6.11.0
    EMOJI_GUILD_ID: 687561835381129256
    GALACTUS_TAG: latest
